package com.idea.exam.domain;

/**
 * Difficulty of a question (analytics / future question-bank filtering). Stored
 * as its {@code name()} string in {@code questions.difficulty_level}.
 */
public enum DifficultyLevel {
    LOW,
    MEDIUM,
    HIGH
}
