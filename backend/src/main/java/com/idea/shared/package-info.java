/**
 * Shared / cross-cutting module.
 *
 * <p>Holds configuration (Security, Redis, JPA), global error handling
 * (RFC-7807 {@code ProblemDetail}) and common utilities reused across modules.
 * Contains no business domain logic of its own.</p>
 */
package com.idea.shared;
